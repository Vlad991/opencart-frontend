let initialState = {
    heading_title: '',
    text_tax: '',
    button_cart: '',
    products: []
};

const featuredReducer = (state = initialState, action) => {
    return state;
};

export default featuredReducer;