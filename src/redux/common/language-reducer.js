let initialState = {
    language: 'EN',
    languages: [
        {
            code: 'EN'
        }
    ]
};

const languageReducer = (state = initialState, action) => {
    return state;
};

export default languageReducer;